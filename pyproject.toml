[project]
name = "lang"
version = "0.1.0"
description = ""
readme = "README.md"
authors = []
requires-python = ">=3.14"
dependencies = []

[project.scripts]
lang = "lang:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
